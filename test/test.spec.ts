import { Connect4 } from '../src/main';

describe("findOutlier", () => {
    it("should return outlier in majority even array", () => {
       // expect(findOutlier([0, 1, 2])).toEqual(1);
       //
    });

    it("should return outlier in majority odd array", () => {
        //expect(findOutlier([0, 1, 2])).toEqual(1);
       //
    });
});